!-- app/views/errors/result.html.erb -->
<div class="min-h-screen bg-black text-cyan-400 overflow-hidden relative">
  <!-- Background Grid Pattern -->
  <div class="absolute inset-0 opacity-20">
    <div class="grid-pattern"></div>
  </div>
  
  <!-- Animated Circuit Lines -->
  <div class="circuit-lines"></div>
  
  <!-- Header Section -->
  <div class="relative z-10 pt-8">
    <!-- Top Status Bar -->
    <div class="flex justify-between items-center px-8 py-4 border-b border-cyan-500 bg-black bg-opacity-80">
      <div class="flex items-center space-x-4">
        <div class="w-3 h-3 bg-red-400 rounded-full animate-pulse"></div>
        <span class="font-mono text-xs tracking-widest">ERROR DETECTED</span>
      </div>
      <div class="font-mono text-xs tracking-widest text-cyan-300">
        DEBUG MODE
      </div>
    </div>

<!-- Main Content -->
    <div class="container mx-auto px-8 py-12">
      <div class="text-center mb-8">
        <h1 class="text-4xl font-bold mb-4 text-cyan-400 font-mono tracking-wider">
          <%= @app_title || "Errorい人" %>
        </h1>
      </div>

      <!-- Error Display -->
      <div class="max-w-2xl mx-auto">
        <div class="bg-gray-900 border-2 border-red-500 rounded-lg p-8 mb-8 shadow-lg shadow-red-500/20">
          <h2 class="text-2xl font-bold text-red-400 mb-6 font-mono text-center">
            今日のエラー
          </h2>
          
          <div class="space-y-6">
            <div class="border-l-4 border-cyan-500 pl-6">
              <h3 class="text-xl font-bold text-cyan-400 mb-2 font-mono">
                <%= @selected_error[:name] %>
              </h3>
              <p class="text-gray-300 leading-relaxed">
                <%= @selected_error[:description] %>
              </p>
            </div>
    <% if @selected_error[:example] %>
              <div class="bg-black border border-gray-700 rounded-lg p-4">
                <h4 class="text-cyan-400 font-mono text-sm mb-2">例:</h4>
                <pre class="text-green-400 font-mono text-sm overflow-x-auto"><code><%= @selected_error[:example] %></code></pre>
              </div>
            <% end %>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <%= link_to draw_errors_path, method: :post, 
              class: "bg-cyan-500 hover:bg-cyan-400 text-black font-bold py-3 px-8 rounded-lg transition-all duration-300 font-mono tracking-wider text-center transform hover:scale-105" do %>
            もう一度引く
          <% end %>
          
          <%= link_to root_path, 
              class: "bg-transparent border-2 border-cyan-500 hover:bg-cyan-500 hover:text-black text-cyan-400 font-